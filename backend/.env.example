# Optional — leave blank to run on SQLite (zero config)
# DATABASE_URL=postgresql://user:password@localhost:5432/erp_demo

# Optional — leave blank and the AI briefing falls back to rule-based summary
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
